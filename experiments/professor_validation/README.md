# Professor's Follow-up Project

Reserved only. No model, dataset conversion, training or validation is implemented.

The supplied future-work prompt concerns GlaBoost and GRAPE longitudinal external
validation. GlaBoost's visit-level classifier is not the DA-SPL report generator.
Do not silently treat them as the same method or start a new longitudinal model.

The current task is the separate IEEE DA-SPL reconstruction in `../paper_method/`.
The original email PDF and supplied requirements remain the authority for a later
scope discussion. No GRAPE data is downloaded or altered at this stage.
